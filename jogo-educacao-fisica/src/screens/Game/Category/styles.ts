import { StyleSheet } from "react-native";

export default StyleSheet.create({
    subtitle: {
        fontSize: 14,
        marginTop: 20,
        marginBottom: 10
    },
    subtitleContainer: {
        flexDirection: "row",
        alignItems: "center",
        justifyContent: "space-between"
    }
});
